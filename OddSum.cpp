#include <bits/stdc++.h>
using namespace std;
int main()
{

    int t;
    while (t--)
    {
        int n, m, area[100][100], count = 0;
        cin >> n >> m;
        for (int i = 0; i < n; i++)
        {

            for (int j = 0; j < m; j++)
            {
            }
        }
    }

    return 0;
}